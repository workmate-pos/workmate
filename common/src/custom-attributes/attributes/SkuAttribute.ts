import { StringAttribute } from '../StringAttribute.js';

/**
 * Attribute containing a SKU.
 * Used for:
 * - Adding SKUs to work order labour line items.
 */
export const SkuAttribute = new StringAttribute('SKU');
