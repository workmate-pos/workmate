import { StringAttribute } from '../StringAttribute.js';

/**
 * Indicates that a line item is an additional charge. Contains the uuid of the line item that this charge belongs to
 */
export const ChargeLineItemUuidAttribute = new StringAttribute('_Charge Line Item UUID');
